services:
  tesseract:
    build:
      context: ./tesseract
    ports:
      - 3000:3000
    networks:
      - app-network

  frontend:
    build:
      context: ./frontend
    ports:
      - 3001:3001
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
